---
title: "GitHub Hot — 26 March 2026"
date: "2026-03-26"
excerpt: "Top 10 trending repos this week — use cases, how to use them, and project ideas for each."
tags: ["GitHub", "Open Source", "Tools"]
coverEmoji: "🔥"
auto_generated: true
---

*Top 10 repos trending on GitHub this week — what they do, why they matter, and how to use them in your projects.*

---

## 1. [slavingia/skills](https://github.com/slavingia/skills)

**3,383 stars this week** · various

A Claude Code plugin that installs 10 business-strategy slash commands based on Sahil Lavingia's Minimalist Entrepreneur framework, turning book principles into interactive AI workflows.

**Use case**

Solo developers and indie hackers often waste weeks building products nobody wants because they skip the validation/community-finding steps. This plugin embeds the exact decision gates from the book directly into your coding environment — so before you write a single line of code, you can run `/validate-idea` in Claude Code and get a structured critique of whether your idea is worth pursuing, or `/mvp` when you're about to over-engineer and need a forcing function to cut scope.

**Why it's trending**

Sahil Lavingia (Gumroad founder) personally published this repo, which drove immediate attention from the indie maker community. It also arrives at peak interest in Claude Code's plugin/skills ecosystem, making it a concrete, real-world demonstration of how agentic coding tools can encode non-technical expertise.

**How to use it**

1. Open Claude Code in your project terminal and run `/plugin marketplace add slavingia/skills` then `/plugin install minimalist-entrepreneur` — no npm, no config files.
2. Start a new side project conversation with `/find-community` — paste in a rough problem area and let Claude map your potential audience before you commit to anything.
3. Once you have a direction, run `/validate-idea` with a one-paragraph description of your concept; it will stress-test assumptions using the book's criteria (do people already pay for a workaround?).
4. When scoping your build, run `/mvp` with your feature list — it will aggressively cut scope and push you toward a manual-first version.
5. Use `/minimalist-review` as a final gut-check before any major decision: launching a paid tier, hiring, or pivoting.

**How I could use this**

1. Run `/validate-idea` on Henry's blog itself — feed it the current feature set and audience assumptions, then publish a post documenting what Claude flagged as unvalidated bets. This turns the tool into actual blog content and demonstrates AI-assisted product thinking publicly.
2. Build a lightweight '/side-project-advisor' page on the blog where visitors paste their idea and Henry's backend calls Claude with the same prompts from `/validate-idea` and `/pricing` — a public-facing tool that drives SEO traffic around 'how to validate a SaaS idea' while showcasing Next.js + Supabase skills.
3. Wire `/processize` into a personal 'pre-build checklist' Supabase table: every time Henry starts a new feature for his blog or a client project, a Claude Code hook runs the command and logs the structured output (manual process steps, success criteria) before any code is written — creating an auditable decision history he can share in case studies.

---

## 2. [zarazhangrui/codebase-to-course](https://github.com/zarazhangrui/codebase-to-course)

**1,645 stars this week** · various

A Claude Code skill that ingests any codebase and outputs a fully self-contained, interactive HTML course explaining how it works — aimed at AI-assisted developers who build but don't deeply understand what they've built.

**Use case**

When you've vibe-coded a Next.js + Supabase blog with AI help but can't confidently explain the auth flow or RLS policies to a hiring manager, this tool generates a scrollable, animated course from your own repo that teaches you your own code. Concrete example: point it at Henry's blog repo and get a course that walks through how the Supabase middleware intercepts requests, with plain-English annotations side-by-side the actual TypeScript.

**Why it's trending**

It's hitting at peak 'vibe coding' discourse — the moment where the AI-assisted developer community is realizing that shipping without understanding creates compounding debt, and this repo offers a concrete escape hatch. The Claude Code skill format is also novel enough to spark curiosity from developers exploring agentic coding workflows.

**How to use it**

1. Install Claude Code CLI and ensure you have API access configured (`npm install -g @anthropic-ai/claude-code`).
2. Clone this repo and register the skill: `claude skills add ./codebase-to-course`.
3. Navigate to the target codebase directory: `cd ~/projects/henry-blog`.
4. Run the skill: `claude 'Generate a course for this codebase' --skill codebase-to-course`.
5. Open the output `course.html` directly in a browser — no server needed, fully offline, self-contained with all animations and quizzes baked in.

**How I could use this**

1. Run this against your own blog repo and publish the resulting `course.html` as a '/how-this-blog-works' page — it's a uniquely compelling portfolio piece that shows architectural thinking without writing a long-form technical post manually.
2. Use it as an interview prep tool: generate a course from a public repo of a company you're interviewing with (e.g., their open-source SDK or starter template) so you can speak fluently about their stack, patterns, and architectural decisions during the technical screen.
3. Build a 'explain my AI feature' workflow: after prototyping a new AI feature (like a semantic search or RAG pipeline) with Cursor or Claude, immediately run this tool on the feature branch to generate a diff-focused mini-course — use it to write the PR description or a blog post explaining the implementation to non-technical readers.

---

## 3. [dontbesilent2025/dbskill](https://github.com/dontbesilent2025/dbskill)

**1,619 stars this week** · various

A Claude Code skill pack distilled from 12,307 tweets into structured business diagnosis workflows — installable as slash commands in Claude Code.

**Use case**

Developers and indie hackers using Claude Code lack structured, repeatable prompting frameworks for business/content decisions. This repo solves that by packaging opinionated methodologies (Adlerian execution diagnosis, Wittgenstein concept deconstruction, Austrian economics debate) as first-class Claude Code skills with a chained workflow — so instead of freeform prompting, you run `/dbs-diagnosis` then `/dbs-benchmark` and get consistent, high-signal output every time. Concrete example: you're stuck on why your blog isn't getting traction — run `/dbs-content` to get a five-dimension content audit, then `/dbs-hook` to generate 10+ opening variants.

**Why it's trending**

Claude Code's slash command / skills ecosystem is exploding right now as developers realize custom skills are the new 'dotfiles' for AI-assisted work — this is one of the first high-quality, opinionated skill packs built from a real knowledge corpus rather than generic prompts, which makes it a reference architecture for building your own.

**How to use it**

1. Install via npx: `npx skills add dontbesilent2025/dbskill` — or manually clone into ~/.claude/skills/ with the one-liner in the README.,2. Open Claude Code in any project and type `/dbs` to hit the auto-router, which reads your input and dispatches to the right sub-skill.,3. For content diagnosis specifically, run `/dbs-content` and paste your blog post or tweet thread — it returns a five-dimension audit (hook, logic, evidence, CTA, distribution fit).,4. If the audit flags a weak opening, the skill auto-recommends `/dbs-hook` — run it to get 10+ rewritten opening lines ranked by pattern type.,5. To build your own skill on top of the knowledge base, pull `知识库/原子库/atoms.jsonl` (4,176 structured knowledge atoms with confidence scores) and use it as RAG context for your own Claude prompts.

**How I could use this**

1. Build a `/blog-diagnosis` Claude Code skill specifically for Henry's blog by forking `/dbs-content` and retraining the five-dimension rubric on dev-blog conventions — feed it a post slug, have it fetch the MDX from Supabase, run the audit, and write structured feedback back as a draft Supabase row in a `post_reviews` table.
2. Create a career tool that pipes a job description through a custom `/jd-deconstruct` skill (modeled on `/dbs-deconstruct`) to strip buzzwords and extract the three real hiring criteria, then compares those against Henry's resume stored in Supabase — outputting a gap score and a tailored bullet list to add.
3. Use the `atoms.jsonl` knowledge base format as a template to build Henry's own 'second brain' knowledge atom store in Supabase — each blog post gets chunked into atoms with topic tags, confidence scores, and skill associations, enabling a `/find-related` slash command in Claude Code that surfaces past writing relevant to whatever Henry is currently drafting.

---

## 4. [louislva/claude-peers-mcp](https://github.com/louislva/claude-peers-mcp)

**1,245 stars this week** · TypeScript

An MCP server that lets multiple Claude Code instances running in parallel terminals discover each other and exchange messages in real time, enabling multi-agent coordination on a single machine.

**Use case**

When you're running Claude Code across 3+ terminals simultaneously — say, one refactoring your Supabase schema, one rewriting React components, and one updating API routes — they have no awareness of each other and will make conflicting changes. claude-peers gives each instance a shared message bus so Claude A can ask Claude B 'what files are you touching?' before writing, preventing merge conflicts and redundant work without manual coordination.

**Why it's trending**

Multi-agent Claude Code workflows exploded this week as developers push past single-context limits by spawning parallel sessions, and this is the first clean MCP-native solution for inter-agent communication that doesn't require a custom orchestrator. It fills a gap Anthropic hasn't addressed natively yet.

**How to use it**

1. Clone and install: `git clone https://github.com/louislva/claude-peers-mcp.git ~/claude-peers-mcp && cd ~/claude-peers-mcp && bun install`
2. Register globally: `claude mcp add --scope user --transport stdio claude-peers -- bun ~/claude-peers-mcp/server.ts`
3. Add alias to your shell: `alias claudepeers='claude --dangerously-load-development-channels server:claude-peers'`
4. Open two terminals and run `claudepeers` in each, then in terminal 1 ask: 'List all peers on this machine'
5. Send a cross-instance query: 'Send a message to peer [id]: what files are you currently editing?' — the response arrives in terminal 2 instantly and Claude can act on it

**How I could use this**

1. Split Henry's blog build into two parallel Claude Code sessions — one owns the Supabase schema + API routes, one owns the Next.js frontend — and use claude-peers so the frontend agent can query the backend agent for the exact shape of a new endpoint before writing the fetch call, eliminating the back-and-forth of copy-pasting types between terminals.
2. For Henry's resume/cover letter career tools: run one Claude agent that specializes in parsing job descriptions and another that owns the resume content, then wire them via claude-peers so the JD agent can push extracted keywords directly to the resume agent mid-session, producing a targeted draft without Henry having to relay context manually between two chats.
3. Build a multi-agent blog post drafting pipeline: one Claude instance browses and summarizes source material (research agent), another holds Henry's writing style guide and tone rules (editor agent), and claude-peers lets the editor agent pull summaries on demand and return a styled draft — effectively a two-node AI writing assistant running entirely in local terminals with no external orchestration service.

---

## 5. [HKUDS/OpenSpace](https://github.com/HKUDS/OpenSpace)

**1,191 stars this week** · Python

OpenSpace is a shared memory/skill layer that sits on top of AI coding agents (Claude Code, Codex, Cursor, etc.) so they learn from past task solutions instead of re-reasoning from scratch every time.

**Use case**

Every time you ask Claude Code to 'add a Supabase RLS policy for a new table,' it burns tokens rediscovering the same pattern it solved last week. OpenSpace stores successful task-solution pairs as reusable skills and shares them across agent sessions — so the second time you run a similar task, the agent retrieves the cached approach and skips the expensive exploration phase. Concrete example: you wire up Supabase auth in your blog once, OpenSpace records the skill, and the next Codex session that hits a similar auth task pulls that solution directly instead of hallucinating a new one.

**Why it's trending**

It dropped this week as a direct response to the Claude Code / Codex / Cursor agent wave — developers are now burning real money on token costs running long agentic sessions and this repo attacks that problem head-on with a claimed 46% token reduction backed by a benchmark. The '$11K earned in 6 hours' hook from a coding contest also created social media traction.

**How to use it**

1. Install: `pip install openspace-agent` (Python 3.12+ required) and run `openspace init` to scaffold a local skill store.
2. Point it at your existing agent — e.g., prefix your Claude Code session: `openspace --agent claude-code --query 'add paginated Supabase query to posts table'`.
3. On first run it executes normally; OpenSpace records the successful solution as a named skill in its store (`~/.openspace/skills/`).
4. On the next similar query, run `openspace --query 'add paginated query to comments table'` — it semantic-matches against stored skills and injects the prior solution as context before calling the LLM, cutting token usage.
5. Optionally connect to the community cloud at `open-space.cloud` to pull skills shared by other developers for common frameworks (Next.js, Supabase, etc.) rather than cold-starting your own skill store.

**How I could use this**

1. Bootstrap an OpenSpace skill store specifically for your Next.js/Supabase blog stack — every time you solve a non-trivial pattern (e.g., Supabase realtime subscriptions, ISR with revalidation, RLS policies for public/private posts), let OpenSpace record it. After a month you'll have a personal agent brain that knows your exact codebase conventions, dramatically cutting the cost of future AI-assisted features.
2. Build a 'Career Agent Memory' workflow: run OpenSpace-backed Codex sessions to generate tailored resume bullets or cover letter sections, record which prompt structures + output formats actually got good results, and reuse those skills for every new job application instead of prompt-engineering from scratch each time.
3. Use OpenSpace's skill-sharing layer to create a public skill pack for the AI blog niche — skills like 'generate MDX blog post with frontmatter from a topic prompt,' 'auto-tag posts using OpenAI embeddings + Supabase pgvector,' or 'scaffold a new blog series landing page.' Publish it to the OpenSpace community cloud and write a blog post about it — instant credibility in both the Next.js and AI agent communities.

---

## 6. [dou-jiang/codex-console](https://github.com/dou-jiang/codex-console)

**1,014 stars this week** · Python

A Python console tool for automating bulk OpenAI account registration, token extraction, and account management — patching the constantly-breaking auth flow.

**Use case**

Developers or operators who need to programmatically manage multiple OpenAI/Codex accounts (e.g., for API key rotation, load balancing across free-tier accounts, or CI environments) hit a wall when OpenAI's registration flow changes silently. This tool handles Sentinel POW challenges, split registration/login flows, OTP parsing from email, and bulk token export — so a workflow that was broken last week actually runs today. Concrete example: you maintain a pool of 50 Codex API keys for a SaaS product and need to re-authenticate all of them after an auth flow change without doing it manually.

**Why it's trending**

OpenAI recently tightened its registration flow with Sentinel POW verification and phone/email binding steps, breaking every existing automation script. This repo is one of the first maintained forks to patch those specific breaking changes, making it immediately useful to anyone whose existing tooling stopped working this week.

**How to use it**

1. Clone and install deps: `git clone https://github.com/dou-jiang/codex-console && cd codex-console && pip install -r requirements.txt`,2. Configure your email provider (Outlook or CloudMail) and proxy settings in the config file — set `proxy_url` in your CPA config if you're behind a firewall.,3. Launch the web UI: `python main.py` — it spins up a Starlette-based local server with a dashboard for account management.,4. Use the batch registration flow to create accounts, then trigger the separate login flow to extract valid tokens — tokens are stored and exportable in Codex format via the export UI.,5. Point the `newApi` upload target in config to your own API gateway or key management service to auto-import harvested tokens.

**How I could use this**

1. Build an API key health-check widget for Henry's blog that silently rotates between a pool of valid tokens using the exported Codex format — so AI-powered blog features (summarization, search) never fail due to a single expired key.
2. Create a personal 'API budget tracker' career tool: use the subscription status check feature to monitor remaining quota across multiple accounts and surface a dashboard showing which projects are burning through tokens fastest.
3. Integrate the token export pipeline into a Supabase Edge Function that auto-refreshes OAuth tokens on a cron schedule using the OAuth refresh logic from this repo — eliminating manual re-auth for any AI feature on Henry's blog that calls OpenAI.

---

## 7. [wong2/weixin-agent-sdk](https://github.com/wong2/weixin-agent-sdk)

**916 stars this week** · TypeScript

A TypeScript SDK that bridges any AI agent (OpenAI, Claude, Codex, etc.) to WeChat's messaging interface via a minimal chat() interface and the ACP protocol.

**Use case**

Chinese developers want to expose AI agents through WeChat — the dominant messaging platform — without reverse-engineering Tencent's proprietary API. This SDK abstracts the WeChat login/message loop so you just implement a single chat() function and your agent is immediately reachable by any WeChat contact, replacing the need to build a custom app or mini-program for simple AI assistant use cases.

**Why it's trending**

Tencent recently launched Clawbot (微信AI助手), and this SDK reverse-engineers the underlying openclaw-weixin package to let anyone plug a custom backend into that interface — perfect timing as Chinese developers race to build WeChat-native AI tools following the Clawbot release.

**How to use it**

1. Install the SDK: `npm install weixin-agent-sdk`
2. Implement the Agent interface with your AI backend:
```typescript
import { login, start, type Agent } from 'weixin-agent-sdk';
import OpenAI from 'openai';

const client = new OpenAI();
const history = new Map<string, {role: string, content: string}[]>();

const myAgent: Agent = {
  async chat({ conversationId, text }) {
    const msgs = history.get(conversationId) ?? [];
    msgs.push({ role: 'user', content: text });
    const res = await client.chat.completions.create({ model: 'gpt-4o', messages: msgs as any });
    const reply = res.choices[0].message.content ?? '';
    msgs.push({ role: 'assistant', content: reply });
    history.set(conversationId, msgs);
    return { text: reply };
  }
};

await login(); // QR code appears in terminal — scan with WeChat
await start(myAgent);
```
3. Scan the QR code with your WeChat account.
4. For ACP-compatible agents (Claude Code, Codex), skip all of the above and just run `npx weixin-acp claude-code`.
5. Send a WeChat message to yourself or to the logged-in account — responses come back via the Clawbot interface.

**How I could use this**

1. Build a WeChat bot that answers questions about Henry's blog posts: feed it the blog content as a system prompt, so when Chinese readers (or recruiters) message his WeChat they get AI-powered answers about his work, projects, and tech stack — effectively a 24/7 portfolio assistant for the Chinese market.
2. Create a job-application assistant accessible via WeChat: implement a chat() handler that takes a job description pasted by the user, calls OpenAI with Henry's resume data as context, and returns a tailored cover letter or skills-gap analysis — useful for Chinese-market job applications where WeChat is the primary professional communication channel.
3. Wire up a Supabase-backed memory layer: use conversationId as the key to store per-contact chat history in Supabase, then expose a Next.js admin dashboard showing which WeChat contacts asked what questions — giving Henry insight into what his audience cares about and feeding that data back into his blog content strategy.

---

## 8. [GAIR-NLP/daVinci-MagiHuman](https://github.com/GAIR-NLP/daVinci-MagiHuman)

**904 stars this week** · Python

daVinci-MagiHuman is a 15B-parameter single-stream transformer that generates synchronized audio-video of realistic humans from text in seconds, fully open source.

**Use case**

Creating talking-head videos with natural lip sync, facial expressions, and body motion without stitching together separate TTS, lip-sync, and video models. For example, you could feed a blog post title + author text prompt and get a 5-second intro video of a presenter speaking it — no studio, no multi-model pipeline juggling.

**Why it's trending**

It dropped this week as a fully open-source alternative to proprietary talking-head APIs (HeyGen, Synthesia) with state-of-the-art benchmark wins and a single-H100 inference speed that makes local deployment actually viable. The 2-second 256p generation benchmark is circulating heavily on AI Twitter.

**How to use it**

1. Clone the repo and install deps: `pip install -r requirements.txt` (requires Python 3.12+, PyTorch 2.9+, and a CUDA GPU).
2. Pull the distilled model weights from HuggingFace: `huggingface-cli download GAIR/daVinci-MagiHuman --local-dir ./models`.
3. Run inference with a text prompt: `python infer.py --prompt 'A professional developer named Henry explains his latest blog post' --duration 5 --resolution 256p --output output.mp4`.
4. For higher quality, swap `--resolution 1080p` and pipe through the included super-resolution model (adds ~36s on H100).
5. Integrate the output `mp4` into your Next.js blog via a standard `<video>` tag or upload to Supabase Storage and reference via a signed URL.

**How I could use this**

1. Auto-generate a 5-second 'Henry introduces this post' talking-head video for each blog article using the post title and a short summary as the text prompt — embed it as an autoplay muted hero above the fold to dramatically increase time-on-page.
2. Build a 'video cover letter' generator for your portfolio site: visitor inputs a job description, your Next.js API route calls the daVinci inference endpoint, and returns a personalized 10-second video of a presenter pitching Henry's skills tailored to that specific role.
3. Create a multilingual blog feature where a single English post auto-generates translated audio-video summaries in Japanese or German using daVinci's multilingual support — store the videos in Supabase Storage keyed by post slug + locale and serve them client-side based on browser language detection.

---

## 9. [opa334/darksword-kexploit](https://github.com/opa334/darksword-kexploit)

**847 stars this week** · Objective-C

A reimplementation of the DarkSword iOS kernel exploit in Objective-C, enabling kernel-level access on iOS 15.0–26.0.1 devices — relevant primarily to jailbreak researchers and security engineers.

**Use case**

This solves the problem of having a portable, readable Objective-C reference implementation of a known kernel exploit chain, useful for security researchers auditing iOS kernel vulnerabilities or building jailbreak toolchains. For example, a researcher validating iOS 26 beta kernel mitigations could use this to test whether the exploit surface still exists before Apple patches it.

**Why it's trending**

iOS 26 (likely the upcoming major release) is mentioned in the supported range, making this immediately relevant as developers and security researchers probe the new kernel ahead of public release. opa334 is a well-known and trusted name in the iOS jailbreak/tweak community (creator of TrollStore, dopamine), which drives significant attention to anything they publish.

**How to use it**

1. This is a low-level kernel exploit — running it requires a device you own and accept full risk of kernel panic/brick on. Do not run on a primary device.
2. Clone the repo and open in Xcode: `git clone https://github.com/opa334/darksword-kexploit`
3. Review the hardcoded offsets in the source — they are currently set for iOS 15.x, so you MUST update offsets for your target iOS version using a tool like `iometa` or kernel offset databases.
4. Build with an entitlement-capable signing identity (paid Apple dev account or a signing bypass like TrollStore on supported devices).
5. Run on-device and monitor output via `idevicesyslog` — a successful run yields kernel r/w primitives which you can extend for your research purposes. Do NOT use this for anything other than personal security research on your own devices.

**How I could use this**

1. Write a technical blog post titled 'What iOS Kernel Exploits Teach Us About Software Security' — use this repo as a concrete case study to explain kernel memory primitives, why hardcoded offsets are a fragility point, and what this means for app-layer developers who assume iOS sandboxing is absolute. This kind of low-level-meets-practical content is rare and drives serious developer traffic.
2. This has no realistic direct application to resume/career tooling — do not force a connection. A more honest angle: add an 'iOS Security' or 'Low-Level Systems' tag to your portfolio if you write the analysis post above, signaling to security-focused employers (Crowdstrike, Trail of Bits, Apple itself) that you engage with this layer of the stack.
3. Build a blog feature where AI summarizes CVEs and public exploits into plain-English risk assessments for indie iOS developers — this repo would be one input. A reader pastes a CVE or links a repo, your Next.js app calls GPT-4o with a structured prompt, and returns 'what this means for YOUR app' rather than raw security jargon. Genuinely useful and differentiating for a dev-focused AI blog.

---

## 10. [ghostty-org/ghostling](https://github.com/ghostty-org/ghostling)

**835 stars this week** · C

Ghostling is a single-file reference implementation showing how to embed Ghostty's battle-tested terminal emulation engine (libghostty-vt) into any C application using Raylib for rendering.

**Use case**

If you want to embed a terminal inside a custom app — a game engine, an Electron alternative, a desktop IDE, or a kiosk UI — you normally have to integrate a full VT parser from scratch or pull in a heavy dependency like xterm.js. libghostty-vt gives you SIMD-optimized VT parsing, Unicode, 24-bit color, and text reflow in a zero-dependency C library, and Ghostling is the canonical minimal example showing exactly how to wire it up with your own renderer.

**Why it's trending**

Ghostty just open-sourced its core as an embeddable library (libghostty-vt), and this repo dropped this week as the first official demo — developers who've been waiting to embed Ghostty-quality terminal emulation into their own apps are racing to understand the API surface before the ecosystem matures.

**How to use it**

1. Clone and build: `git clone https://github.com/ghostty-org/ghostling && cd ghostling && make` — requires Raylib and libghostty-vt headers/libs installed.,2. Read `main.c` top-to-bottom — it's intentionally a single file. Focus on the lifecycle: `ghostty_surface_create()`, the render loop calling `ghostty_surface_draw()`, and how input events are forwarded via `ghostty_surface_key()`.,3. Swap Raylib out for your own renderer by replacing the `DrawTextCodepoint` calls with whatever graphics API you target (SDL, OpenGL, a web canvas via WASM, etc.) — libghostty-vt itself has zero renderer coupling.,4. To embed in a Node/Electron context, compile libghostty-vt to a native addon or WASM module, then bind the C API using `napi` or `Emscripten` — Ghostling's thin wrapper shows you exactly which API calls are mandatory.,5. Watch the libghostty-vt header (`ghostty.h`) for the full API surface — `ghostty_surface_read_stable_pin` gives you direct access to terminal cell data (char + style) for custom rendering pipelines.

**How I could use this**

1. Build an in-browser interactive terminal widget for your blog using libghostty-vt compiled to WASM — readers could run real CLI demos of your open-source tools (linters, generators, scripts) directly in a blog post without leaving the page, with accurate VT rendering instead of xterm.js quirks.
2. Create a sandboxed 'résumé CLI' career tool: a fake terminal embedded in your portfolio site that responds to commands like `cat henry-resume.txt`, `git log --oneline career`, or `./run-demo.sh` — powered by libghostty-vt for authentic terminal aesthetics and keyboard shortcuts, making it memorable to recruiters who are also developers.
3. Embed a persistent terminal pane inside an AI coding assistant feature on your blog — pipe streaming output from a Supabase Edge Function (calling an LLM) directly into a libghostty-vt surface, so AI-generated code explanations or diffs render with proper ANSI color, cursor control, and scroll history instead of plain pre-formatted text.
